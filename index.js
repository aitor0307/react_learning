import React from 'react';
import ReactDOM from 'react-dom';
import './style.css';

ReactDOM.render(
  <h1>Hello, React!</h1>,
  document.getElementById('root')
);

ReactDOM.render(
  <h1>Everybody, everybody, it's getting to it, get started! Let's get started in here!</h1>,
  document.getElementById('test')
);
